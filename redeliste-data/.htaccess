# Redeliste-Daten nicht direkt über das Web ausliefern (Zugriff nur über redeliste-sync.php)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
